minutetoure.duckdns.org {
	encode gzip

	# Proxy API to backend
	@api path /api/*
	handle @api {
		reverse_proxy backend:8000
	}

	# Everything else → frontend (serves SPA with its own try_files)
	handle {
		reverse_proxy frontend:80
	}
}
